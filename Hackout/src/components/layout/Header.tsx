import { Bell, Search } from 'lucide-react'; import { useState } from 'react'; import { useAuth } from '../../hooks/useAuth'; import { useNotifications } from '../../hooks/useNotifications'; import { Avatar } from '../shared'; import { NotificationCenter } from './NotificationCenter';
export function Header(){const [open,setOpen]=useState(false);const {user,role}=useAuth();const {unreadCount}=useNotifications();return <><header className="glass-panel header"><div className="header-title"><span className="eyebrow">{role?.replace('_',' ')} workspace</span></div><div className="glass-inset" style={{display:'flex',alignItems:'center',padding:'0 9px',flex:1,maxWidth:380}}><Search size={16} color="#94a3b8"/><input className="header-search" placeholder="Search your workspace" aria-label="Search your workspace"/></div><div className="header-actions"><button className="icon-btn" aria-label="Open notifications" onClick={()=>setOpen(true)}><Bell size={19}/>{unreadCount>0&&<i className="badge-count">{unreadCount}</i>}</button>{user&&<Avatar name={user.name} small/>}</div></header><NotificationCenter open={open} onClose={()=>setOpen(false)}/></>}

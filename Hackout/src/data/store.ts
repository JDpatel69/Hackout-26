import { users } from './users.fixtures'; import { farms, documents } from './farms.fixtures'; import { verificationRequests } from './verificationRequests.fixtures'; import { carbonProjects } from './carbonProjects.fixtures'; import { investments, transactions } from './investments.fixtures'; import { datasets, researchReports } from './datasets.fixtures'; import { notifications } from './notifications.fixtures';
export const store = { users: [...users], farms: [...farms], documents: [...documents], requests: [...verificationRequests], projects: [...carbonProjects], investments: [...investments], transactions: [...transactions], datasets: [...datasets], researchReports: [...researchReports], notifications: [...notifications] };
